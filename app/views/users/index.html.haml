%p#notice= notice
%h1 Listing users
%table
  %thead
    %tr
      %th Name
      %th Company
      %th{:colspan => "3"}
  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.company
        %td= link_to 'Show', user
        %td= link_to 'Edit', edit_user_path(user)
        %td= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }
       

= link_to 'New User', new_user_path
|
- if current_user
  %td= link_to 'Link Badge with User', link_badge_user_path(current_user.id)
