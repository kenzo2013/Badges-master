%p#notice= notice
= container_tag do
  %h1= @user.infos
= container_3_tag @user.badges, rows: {cols: {class: 'text-center'}} do |badge|
  %h2= badge.name
  .symbol
    = fa_icon badge.fa_symbol
= container_tag id: 'links', class: 'text-center' do
  = btn_to 'Edit', edit_user_path(@user)
  = btn_to 'Back', users_path
